---
version: "3.8"

services:
  webapp:
    build:
      context: ..
      dockerfile: docker/Dockerfile
    container_name: sample-webapp
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=production
      - PORT=3000
    restart: unless-stopped
